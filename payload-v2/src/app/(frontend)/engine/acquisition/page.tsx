/**
 * /engine/acquisition — Data import page.
 *
 * Thin shell: only imports and renders the ImportPage component.
 */

import ImportPage from '@/features/engine/acquisition/components/ImportPage'

// ============================================================
// Page
// ============================================================
export default function Page() {
  return <ImportPage />
}
